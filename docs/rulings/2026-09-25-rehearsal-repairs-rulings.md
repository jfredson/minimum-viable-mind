# Ruling 2026-09-25: the five decisions from the Weekend 1 rehearsal repairs

*Recorded 2026-09-25 (Pacific), in a Cowork planning session. Mixed authorship:
the findings and the decisions owed are session (c)'s,
`docs/2026-09-26-rehearsal-repairs.md` on pull request 52 (branch
`worktree-w1c-rehearsal-repairs`, commit `e0626b2`), still owed its check under
the pairing rule when this was recorded; each decision was put to John in
plain language with a recommendation and he ruled "Agreed on all". The choices
are his; none of the wording is his drafting. No compute was launched and no
money was spent under this ruling. Every number below is the findings file's,
which cites the committed output files under
`experiments/rehearsal-successor-measure/out-repairs/`.*

*Written under the workspace plain-language rule. Nothing here edits registered
text, protocol text or an earlier ruling; where an earlier ruling is refined,
that is said. This ruling refines `docs/rulings/2026-09-26-weekend-1-queue.md`
(pages 3, 4 and 5) and is read with it.*

---

## What was ruled

1. **The named-other condition (page 4).** Both training redesigns failed the
   pre-stated line (curriculum 0 of 3 seeds, loss re-weighting 0 of 3, the
   unchanged recipe 1 of 3; findings section 2). Page 4's fallback (d) stands
   for version 2 of the successor proposal: the registration text records the
   failure, that doubling the budget and both training changes did not fix it,
   and that the staggered first run bounds the money. **Separately, redesign
   (c), a grammar change, is attempted at toy scale on the laptop at $0 on
   2026-09-26**, as its own session, with its pass line pre-stated before the
   run: the named-other condition above the page 1b bar on at least two seeds
   of three on the free toy arm, with the own-directed condition not degraded
   below its current level. If it clears, version 2 is amended before its Gate
   A with the grammar change and the rehearsal items it re-opens (R-1 to R-6)
   re-run; if it does not, (d) is what registers. The findings' diagnosis
   (both action turns receive the same acting signal) is the starting point,
   not a ruling.

2. **Arm M, the fourth arm (page 5).** It passed the pre-stated line
   (chance-corrected readings 0.48 to 0.53 on all three seeds, within 0.04 of
   its construction's prediction; findings section 5). **It is folded into the
   main registration**, under page 5's option (iii) and page 6's $450 envelope:
   three registered runs at the ledger's per-run figures ($32 to $44), which
   with the two releases recomputed from the measured seconds per step
   ($161.90, `docs/2026-09-25-rented-slice-attempt-2-findings.md`) leaves the
   programme at about $434 of $450. The registration describes it as what it
   is: a mixture by item, each action going wholly through the separable or the
   entangled route, about three fifths entangled, not partial separation within
   a trial.

3. **The degenerate-set exclusion (page 3, refined).** Page 1e's exclusion of
   "every layer at every position" is widened: **any site set that spans every
   position at any layer is excluded**, because copying all positions at a
   layer hands the donor's whole forward pass downstream (the rule nominated
   such a set on arm C, seeds 0 and 1; findings section 3). The site count is
   the rule's output (296 on the toy, 1,816 on the registered 12-layer model
   with the toy's position sets); the packet's 176 was hand arithmetic and is
   superseded.

4. **"The smallest layer set that clears it" (page 1c, refined).** The reading
   registered is the one implemented: the smallest layer set that clears the
   four-fifths floor, per position set, then the highest ownership-only share
   among those. The two readings differed on 7 of 12 arm-and-seed pairs by
   0.02 or less (findings section 3).

5. **Control 2.** As the proposal words it (section 7.3, item 2) it cannot run
   on arms T and M, which hold the named agent outside the running state by
   construction, and returns no verdict on an arm that has not learned the
   named-other condition (findings section 4). **It is recorded as not
   applicable on arms T and M**, and on arms C and F it is carried as a
   reported check with its pass line pre-stated, run only once the arm has
   learned the named-other condition, not as a control that holds. Its
   mechanism is not reworded this weekend.

Also ruled, from the findings' "what this means" list: a chance-corrected
reading above 1.0 is reported as observed and not capped (section 6.2); the
page 3 rider stays in the reporting table and returns "no verdict" where the
whole-state transplant moves nothing at arm T's site set, which is itself the
report.

## What this changes, and where

Version 2 of the successor proposal, sections 4 (the grammar, if item 1's
attempt clears), 5 (arm M as a fourth arm, every arm table), 6.4 and 7.2 (items
3 and 4), 7.3 (control 2), 8.1 (the named-other risk on the record), 12.4 (arm
M's three runs in the second release). The weekend roadmap's section 7 carries
the (c) attempt as a 2026-09-26 item.

## What this file does not do

It edits nothing. Session (c)'s findings are still owed their check; if that
check moves a number above, the ruling is re-read against the checked figure
and this file is annotated, not rewritten.

---

## Annotations 2026-09-26, after the check on pull request 58

*Added 2026-09-26. Session (c)'s findings have now had their check:
`experiments/06-mvm-0a-constructed-self-index/reviews/2026-09-26-rehearsal-repairs-check-claude-worktree.md`,
merged to main by pull request 58 (commit `d216dbc`), which re-ran the repairs
from clean and read this ruling against the result, number by number. As the
section above says, this file is annotated, not rewritten: every sentence above
stands as recorded. Each entry below quotes the sentence it annotates and gives
the checked figure, with the check file's section. Nothing here is a new ruling;
the five decisions stand unchanged, and none of the corrections below bears on
any of them (check file, section 5.1).*

1. **Item 2: "($161.90, `docs/2026-09-25-rented-slice-attempt-2-findings.md`)".**
   When this ruling was recorded, that findings file (the rented slice's second
   attempt) was on an unmerged branch, so the figure's source was not yet on
   main. It is now: pull request 51 merged it (commit `9f802db`). The figure
   itself is as cited; that file's section 7 gives $161.90 (check file,
   section 5).

2. **Item 2: "leaves the programme at about $434 of $450".** That is the top of
   the range, not the single figure. The attempt-2 findings leave about $60.0 of
   $450 after both releases, so about $390 is committed; adding arm M's three
   runs at $32 to $44 gives **$422 to $434 of $450** (check file, section 5,
   where the arithmetic is shown).

3. **Item 4: "The two readings differed on 7 of 12 arm-and-seed pairs by 0.02
   or less".** The committed run's files show **8 of 12**, largest gap 0.0200
   (check file, section 4.1). The check's re-run shows **6 of 12, by up to
   0.0233** (check file, sections 4.1 and 5.1). No exact count is a property of
   the code; about half the pairs, by about 0.02, is what both runs support.

   **Item 2: "within 0.04 of its construction's prediction".** The largest miss
   on the committed run is **0.0418**, just over 0.04 (check file, section 4.3);
   on the re-run the misses are 0.0405, 0.0408 and **0.0530** (check file,
   section 5.1). Both runs are well inside the pre-stated pass line of 0.10, so
   arm M's pass stands.

   **Item 3: "the rule nominated such a set on arm C, seeds 0 and 1".** That is
   true of the committed run only. On the check's re-run the rule's pick of an
   every-position set landed on **arm F, seed 0** instead (check file,
   section 5.1). This strengthens the reason for the widened exclusion: the rule
   picks such a set on a second arm in a second run.

4. **Item 3: "The site count is the rule's output (296 on the toy, 1,816 on the
   registered 12-layer model with the toy's position sets)".** Those two counts
   are the ones under page 1e's old, narrow exclusion. Under this item's own
   widened exclusion, keeping the toy's five position sets and counting only the
   "every position" set as spanning every position, the counts are **240 on the
   toy and 1,456 on the registered model** (check file, section 5, where the
   command that computes them is shown). The check notes that the true count
   still depends on the position list the registration prints. The principle
   that the count is the rule's output is unaffected.

   **Item 3: "the packet's 176 was hand arithmetic".** The 176 was not hand
   arithmetic: it is the size of the family of site sets actually run, listed by
   hand in the code and checked there (`repairs.py`: `assert len(SITE_SETS) ==
   44 and FAMILY_SIZE == 176`), and recorded in every `nominate_base_*.json`
   (check file, section 5). "Hand-listed", not "hand arithmetic". It is still
   superseded, as item 3 says.

5. **Also ruled: the page 3 rider "returns 'no verdict' where the whole-state
   transplant moves nothing at arm T's site set".** That describes arms C and F,
   not arm M. On arm M, copying the donor's whole state at arm T's site set
   moves the action in **about 0.41** of trials against about 0.01 untouched, on
   both the committed run and the re-run; its "no verdict" there is a miss of
   the four-fifths floor, not nothing moving (check file, section 4.2). The
   rider's "no verdict" therefore means two different things across the arms,
   and the report should say which.
