# Stage 0 core — enough to run the smoke test (00_setup_check.py).
# Apple Silicon (MPS). Python 3.11 or 3.12 recommended.
torch>=2.4
transformers>=4.44
accelerate>=0.33
huggingface_hub>=0.24
sentencepiece>=0.2       # Gemma tokenizer
anthropic>=0.111         # held-out judge for the S (self-report) battery — a
                         # different model from the one under test (see
                         # experiments/01-.../src/judge.py and the rubric's
                         # judge-contamination control)
google-genai>=1.0        # held-out red team (Gemini 3.1 Pro) — a different model
                         # family from the builder and the judge, for adversarial
                         # design review (see experiments/01-.../src/red_team.py).
                         # Needs GEMINI_API_KEY in .env; not used by --dry-run.
