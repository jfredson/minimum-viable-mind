$ ../../../.venv/bin/python grammar.py --self-test --named-other-acting 0
NAMED_OTHER_ACTING = 0
grammar.py self-test
  [PASS] matched pairs are token-for-token identical
  [PASS] matched pairs differ in the acting channel
  [PASS] matched property 1 — four distinct candidate values per item  distinct counts seen: [4]
  [PASS] matched property 2 — distance distributions match  largest bin difference 0.0098 over 6000 episodes; mean distance own 5.010 vs other 4.928
  [PASS] matched property 3 — one supervised position of each kind
  [PASS] matched property 3 — every scored position holds the mask word
  [PASS] matched property 4 — the same successor rule in both conditions
  [PASS] one-scored-token check — exactly two scored tokens per episode
  [PASS] one-scored-token check — scored at the mask position, unshifted
  [PASS] even-split rule — a paired batch has an even number of rows
  [PASS] no turn-count cue — every agent takes the same number of turns
  [PASS] no name badge — the own-directed turn shows only the self word
  [PASS] model identity spread over all four agent slots  share per agent slot [0.251, 0.25, 0.252, 0.246]
  [PASS] fresh episodes are combinations seen in neither other set  600 fresh combinations, none of them among 3000 training ones
  [PASS] the unseen-vocabulary pool shares no marker word or item with training
  [PASS] collision set supplies a same-value cell for control 6  30 of 400 pairs have donor and recipient dictating the same value
  [PASS] the setting leaves every token unchanged
  [PASS] the setting changes the acting channel only on the seven named-other action-turn positions  14000 positions differ over 2000 episodes
  [PASS] acting channel at the scored positions: own-directed [1], named-other [0]

all checks passed

$ ../../../.venv/bin/python grammar.py --self-test
NAMED_OTHER_ACTING = 1
grammar.py self-test
  [PASS] matched pairs are token-for-token identical
  [PASS] matched pairs differ in the acting channel
  [PASS] matched property 1 — four distinct candidate values per item  distinct counts seen: [4]
  [PASS] matched property 2 — distance distributions match  largest bin difference 0.0098 over 6000 episodes; mean distance own 5.010 vs other 4.928
  [PASS] matched property 3 — one supervised position of each kind
  [PASS] matched property 3 — every scored position holds the mask word
  [PASS] matched property 4 — the same successor rule in both conditions
  [PASS] one-scored-token check — exactly two scored tokens per episode
  [PASS] one-scored-token check — scored at the mask position, unshifted
  [PASS] even-split rule — a paired batch has an even number of rows
  [PASS] no turn-count cue — every agent takes the same number of turns
  [PASS] no name badge — the own-directed turn shows only the self word
  [PASS] model identity spread over all four agent slots  share per agent slot [0.251, 0.25, 0.252, 0.246]
  [PASS] fresh episodes are combinations seen in neither other set  600 fresh combinations, none of them among 3000 training ones
  [PASS] the unseen-vocabulary pool shares no marker word or item with training
  [PASS] collision set supplies a same-value cell for control 6  30 of 400 pairs have donor and recipient dictating the same value
  [PASS] the setting leaves every token unchanged
  [PASS] the setting changes the acting channel only on the seven named-other action-turn positions  14000 positions differ over 2000 episodes
  [PASS] acting channel at the scored positions: own-directed [1], named-other [1]

all checks passed

$ ../../../.venv/bin/python arms.py --self-test
arms.py self-test
  [PASS] arm T — forward runs and is the right shape  950,879 parameters
  [PASS] arm T — loss is finite and gradients flow
  [PASS] arm C — forward runs and is the right shape  1,301,191 parameters
  [PASS] arm C — loss is finite and gradients flow
  [PASS] arm F — forward runs and is the right shape  1,265,191 parameters
  [PASS] arm F — loss is finite and gradients flow
  [PASS] arm T — the ownership block is carried unchanged through every layer
  [PASS] arm T — content never reads the ownership block
  [PASS] arm T — but the action head does read it
  [PASS] arm C — the ownership signal reaches every block as a scale-and-shift
  [PASS] arm C — no dedicated ownership dimensions exist in its running state
  [PASS] the ownership answer is flat before the model's first own turn  first own-turn token at position 21
  [PASS] the ownership answer is sharp and correct by the action position  model is agent slot 3, read 0.9990
  [PASS] twins differ only in the acting channel
  [PASS] arm T — the twins' states differ, so ownership does reach the state
  [PASS] arm C — the twins' states differ, so ownership does reach the state
  [PASS] arm F — the twins' states differ, so ownership does reach the state

all checks passed

$ ../../../.venv/bin/python measure.py --self-test
measure.py self-test — the arithmetic on cases chosen to break it
  [PASS] fully separable: the subspace does everything  whole 0.9000, ownership-only 0.9000 -> valid, degree 0.0000
  [PASS] fully entangled: the subspace does nothing  whole 0.9000, ownership-only 0.0000 -> valid, degree 1.0000
  [PASS] half and half  whole 0.8000, ownership-only 0.4000 -> valid, degree 0.5000
  [PASS] the denominator is exactly zero  whole 0.0000, ownership-only 0.0000 -> no verdict
  [PASS] the denominator is just under the floor  whole 0.2999, ownership-only 0.1000 -> no verdict
  [PASS] the denominator is just over the floor  whole 0.3001, ownership-only 0.1000 -> valid, degree 0.6668
  [PASS] the subspace beats the whole state  whole 0.5000, ownership-only 0.8000 -> negative, degree -0.6000
  [PASS] both at the ceiling  whole 1.0000, ownership-only 1.0000 -> valid, degree 0.0000
  [PASS] whole state at the ceiling, subspace at zero  whole 1.0000, ownership-only 0.0000 -> valid, degree 1.0000
  [PASS] both zero  whole 0.0000, ownership-only 0.0000 -> no verdict

  the review's two systems, both with half the identity-driven difference outside the subspace:
    strong transplant  whole 0.9000 ownership-only 0.5125: registered form 0.4306, floor-corrected form 0.5000
    weak transplant    whole 0.3500 ownership-only 0.2375: registered form 0.3214, floor-corrected form 0.5000
  [PASS] the registered form gives the two systems different readings  0.4306 against 0.3214, a gap of 0.1091
  [PASS] the floor-corrected form gives them the same reading  0.5000 against 0.5000
  [PASS] every share pair from zero to one returns a finite number or no verdict  10201 pairs swept, 0 non-finite
  [PASS] a floor of zero is refused  the unsatisfiable-denominator defect cannot be reintroduced by passing a floor of zero
  [PASS] a score of -0.1 is refused
  [PASS] a score of 1.1 is refused
  [PASS] a score of nan is refused
  [PASS] a score of inf is refused

all checks passed
