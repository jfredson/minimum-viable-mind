# DRAFT upstream packet — a wired self-channel can prevent learning general other-modeling (fires ONLY if 30M reproduces the 10M signature)

*2026-08-15. NOT A PROPOSAL YET. Pre-drafted skeleton for the
H_shortcut-starvation branch; if H_scale wins, delete unused. If it fires:
finalize numbers, then deliver as
`sentient-horizons/ops/proposals/2026-08-XX-mvm-shortcut-starvation.md`
(PROPOSED — John ratifies; canon untouched), same route as the RT-17
packet (`af36de8`). The ladder also STOPS per `pilot-a1-findings.md`
step 2 — the loss is structural, not scale — and the fix opens a
registered amendment cycle (candidate: rebalancing query supervision).*

## The finding (fill from the 30M record)

Two scales, same architecture family, same curriculum, same budget rule:

| | 10M A1 | 30M A1 |
|---|---|---|
| T_sr (own-act retrieval) | 0.96 (instant) | [FILL] |
| T_si (other-binding) | 0.37 — never transitions | [FILL] |
| T_sr_rev (revision retrieval) | 0.00 flat | [FILL] |
| T_state / T_syntax | 1.00 / 1.00 | [FILL] |

v1.0 (on-policy fill pipeline, same 10M scale) hit T_si 0.97 on the same
battery/budget — the capacity was never the problem. The variable that
changed is *how authorship enters*: v1.0 forced the model to infer
ownership from statistics, which required building general
marker-binding machinery that T_si then reused; A1 hands ownership over
a wired acting channel (motor-copy at emission positions), and the
binding machinery [FILL: was never built / was built only for self].

## Why this goes upstream

The corpus's self-location/self-reference distinction (Calibration
Problem ch. 5; RT-17 packet) predicted ownership cannot be a statistic
in a token-only interface. This result is the uncomfortable dual: give
the system its self-location for free, and the *pressure* that would
have built general other-modeling disappears — the wired self-channel
starves the very capacity it was meant to ground. Formation lesson in
one line: **a self that is given rather than earned models no one.**
[Sharpen against ch. 5's actual wording before delivery.]

## Wager (must be stated before delivery)

This claim predicts a curricular fix that restores other-binding
pressure (e.g. rebalanced query supervision forcing other-agent
retrieval) lets the SAME 30M architecture learn T_si to ceiling with the
acting channel intact. If a rebalanced-supervision run also fails T_si,
the starvation story is wrong and the failure is architectural — this
packet retracts.

## Checklist before delivery

- [ ] 30M verdict adjudicated H_shortcut-starvation (John)
- [ ] Numbers filled from `run_post_pilot.sh` output + jsonl
- [ ] Ch. 5 citation verified by path + heading (house rule)
- [ ] VCP not required (ops proposal, not public prose) — confirm
- [ ] Ladder-stop recorded in STATUS + ledger (no 100M run)
